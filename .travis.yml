sudo: false # Run on the new AWS/Docker-based Travis CI infrastructure
language: go
go:
  - 1.4

before_install:
  - go get -v github.com/docker/docker/pkg/units  # Travis wants this package, for whatever reason ...
